# URL de base de l'API Dorloter (Rust). En dev on passe par le proxy Vite (/api), donc
# laisser vide suffit. En prod, pointer sur l'API deployee.
VITE_API_URL=

# Cible du proxy Vite en dev (defaut : http://localhost:8080)
VITE_API_PROXY=http://localhost:8080
