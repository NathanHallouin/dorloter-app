# Fichiers exclus du contexte de build Vercel.
# Vercel déploie depuis le repo entier (monorepo) — sans cette liste,
# il embarque apps/mobile (RN/Expo, ~400 Mo) dans le bundle serverless
# et dépasse la limite de 250 Mo.

# App mobile : aucune dépendance partagée avec apps/web (ils
# communiquent via @dorloter/api-client uniquement).
apps/mobile

# Tests / artefacts CI
apps/web/playwright-report
apps/web/test-results
apps/web/.next/cache

# Docs
docs

# Scripts shell hors build
scripts
